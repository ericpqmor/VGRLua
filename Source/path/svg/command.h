#ifndef RVG_PATH_SVG_COMMAND_H
#define RVG_PATH_SVG_COMMAND_H

#include "path/svg/token.h"

namespace rvg {
    namespace path {
        namespace svg {
            namespace command {

// declares one Token for each SVG command
extern Token T, t, R, r, A, a, C, c, H, h, L, l, M, m, Q, q, S, s, V, v, Z;

} } } } // namespace rvg::path::svg::command

#endif
